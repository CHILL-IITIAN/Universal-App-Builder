# Student Manager

A student management application built with Flutter.

## Getting Started

```bash
flutter pub get
flutter run
```

## Building

```bash
flutter build windows --release
flutter build apk --release
flutter build web --release
```
