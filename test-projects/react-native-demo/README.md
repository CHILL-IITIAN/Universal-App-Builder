# FitApp

A fitness tracking mobile application built with React Native.

## Development

```bash
npm install
npm start
npm run android   # or npm run ios
```

## Building

```bash
cd android && ./gradlew assembleRelease   # Android APK
cd ios && xcodebuild -scheme FitApp       # iOS (macOS only)
```
