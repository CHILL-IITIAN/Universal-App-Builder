# Hybrid App (Multi-Framework Test)

This project intentionally contains artifacts from multiple frameworks:
- Flutter (pubspec.yaml + lib/)
- Tauri (src-tauri/)
- Electron (package.json + electron dep)

This tests the "multiple frameworks detected" UI flow where the user
must choose which framework to build with.
